export interface product {
    id: number,
    name: string,
    price: number,
    category: string,
    gender: string,
    sizes: string[],
    images: string[],
    color: string,
    seller: string,
    addedDate: string,
    stock: number
}